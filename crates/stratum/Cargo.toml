[package]
name        = "stratum"
version     = "0.1.0"
edition     = "2021"
description = "World orchestration layer for the Pulsar engine — levels, spatial streaming, cameras, render views."

[dependencies]
glam = { workspace = true }
log  = { workspace = true }
